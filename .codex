Repo conventions:
- Branch names should use slash prefixes such as `feat/...`, `fix/...`, or `chore/...`; do not use hyphenated prefixes such as `feat-...`, `fix-...`, or `chore-...`.
- When staging changes, use `git add --all` unless the user explicitly says to stage only specific files.
